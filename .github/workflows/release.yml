name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'

      - name: Build binaries
        run: make build-all

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            bin/rd-downloader-linux-amd64
            bin/rd-downloader-linux-arm64
            bin/rd-downloader-darwin-amd64
            bin/rd-downloader-darwin-arm64
            bin/rd-downloader-windows-amd64.exe
          generate_release_notes: true
